{
	$I = ?my_id ();
 	while ( ?active_players () <> 1)
	{
		if ( ?money (I) - ?raw_price () * ?factories () > 0 )  {
			buy (?factories (I), ?raw_price ());
		}
		sell (?factories (I), ?production_price ());
		prod ( ?factories (I);

		turn ();
	};
}
