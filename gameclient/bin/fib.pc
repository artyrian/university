{
	$m := 10;
	
	$f [0] := 1;
	$f [1] := 1;
	$i := 2;

	while ($f [$i] < $m ) do {
		$f [$i] := $f [$i - 1] + $f [$i-2];	
		$i := $i + 1;
	};

	$j := 0;
	while ( ( $j < $i) & ( $j = $i ) ) do  {
		print ($f [$j] );
	};
}
